# JssTextWithEntityName

This JSS component abstraction serves as a find/replace tool for entity names.

Some CMS fields contain the placeholder term `{{EntityName}}` as part their text string value.

We find and replace that placeholder with an EntityName value which is unpacked from Sitecore context.