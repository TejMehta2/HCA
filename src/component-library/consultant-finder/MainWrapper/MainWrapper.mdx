import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as MainWrapperStories from './MainWrapper.stories'

<Meta of={MainWrapperStories} />

# MainWrapper

## Description

The MainWrapper component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<MainWrapper></MainWrapper>
```

## Example 

<Canvas of={MainWrapperStories.Default} />
<Controls of={MainWrapperStories.Default} />
