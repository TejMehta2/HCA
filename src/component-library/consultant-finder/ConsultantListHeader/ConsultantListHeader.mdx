import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as ConsultantListHeaderStories from './ConsultantListHeader.stories'

<Meta of={ConsultantListHeaderStories} />

# ConsultantListHeader

## Description

The ConsultantListHeader component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<ConsultantListHeader></ConsultantListHeader>
```

## Example 

<Canvas of={ConsultantListHeaderStories.Default} />
<Controls of={ConsultantListHeaderStories.Default} />
