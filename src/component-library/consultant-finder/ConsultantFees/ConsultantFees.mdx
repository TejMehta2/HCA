import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as ConsultantFeesStories from './ConsultantFees.stories'

<Meta of={ConsultantFeesStories} />

# ConsultantFees

## Description

The ConsultantFees component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<ConsultantFees></ConsultantFees>
```

## Example 

<Canvas of={ConsultantFeesStories.Default} />
<Controls of={ConsultantFeesStories.Default} />
