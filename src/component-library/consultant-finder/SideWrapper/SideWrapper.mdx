import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as SideWrapperStories from './SideWrapper.stories'

<Meta of={SideWrapperStories} />

# SideWrapper

## Description

The SideWrapper component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<SideWrapper></SideWrapper>
```

## Example 

<Canvas of={SideWrapperStories.Default} />
<Controls of={SideWrapperStories.Default} />
