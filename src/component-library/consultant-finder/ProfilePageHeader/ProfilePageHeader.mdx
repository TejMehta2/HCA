import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as ProfilePageHeaderStories from './ProfilePageHeader.stories'

<Meta of={ProfilePageHeaderStories} />

# ProfilePageHeader

## Description

The ProfilePageHeader component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<ProfilePageHeader></ProfilePageHeader>
```

## Example 

<Canvas of={ProfilePageHeaderStories.Default} />
<Controls of={ProfilePageHeaderStories.Default} />
