import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as LocationsStories from './Locations.stories'

<Meta of={LocationsStories} />

# Locations

## Description

The Locations component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<Locations></Locations>
```

## Example 

<Canvas of={LocationsStories.Default} />
<Controls of={LocationsStories.Default} />
