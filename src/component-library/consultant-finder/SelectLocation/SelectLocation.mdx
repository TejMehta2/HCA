import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as SelectLocationStories from './SelectLocation.stories'

<Meta of={SelectLocationStories} />

# SelectLocation

## Description

The SelectLocation component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<SelectLocation></SelectLocation>
```

## Example 

<Canvas of={SelectLocationStories.Default} />
<Controls of={SelectLocationStories.Default} />
