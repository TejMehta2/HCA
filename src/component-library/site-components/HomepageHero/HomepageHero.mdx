import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as HomepageHeroStories from './HomepageHero.stories'

<Meta of={HomepageHeroStories} />

# HomepageHero

## Description

The HomepageHero component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<HomepageHero></HomepageHero>
```

## Example 

<Canvas of={HomepageHeroStories.Default} />
<Controls of={HomepageHeroStories.Default} />
