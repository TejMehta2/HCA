import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as SearchWrapperStories from './SearchWrapper.stories'

<Meta of={SearchWrapperStories} />

# SearchWrapper

## Description

The SearchWrapper component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<SearchWrapper></SearchWrapper>
```

## Example 

<Canvas of={SearchWrapperStories.Default} />
<Controls of={SearchWrapperStories.Default} />
