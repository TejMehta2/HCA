import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as TabsBlockStories from './TabsBlock.stories'

<Meta of={TabsBlockStories} />

# TabsBlock

## Description

The TabsBlock component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<TabsBlock></TabsBlock>
```

## Example 

<Canvas of={TabsBlockStories.Default} />
<Controls of={TabsBlockStories.Default} />
