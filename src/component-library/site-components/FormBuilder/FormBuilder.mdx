import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as FormBuilderStories from './FormBuilder.stories'

<Meta of={FormBuilderStories} />

# FormBuilder

## Description

The FormBuilder component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<FormBuilder></FormBuilder>
```

## Example 

<Canvas of={FormBuilderStories.Default} />
<Controls of={FormBuilderStories.Default} />
