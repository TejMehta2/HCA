import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as FiltersStories from './Filters.stories'

<Meta of={FiltersStories} />

# Filters

## Description

The Filters component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<Filters></Filters>
```

## Example 

<Canvas of={FiltersStories.Default} />
<Controls of={FiltersStories.Default} />
