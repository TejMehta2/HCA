import { Canvas, Controls, Meta } from '@storybook/blocks';

# Accordion

## Description

The `Accordion` component is only used when nested in the `Accordions` component which controls the functionality of the Accordions as a group, and should be used even when using a single accordion.

For examples please refer to the Acccordions component.
