import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as AccordionGroupStories from './AccordionGroup.stories'

<Meta of={AccordionGroupStories} />

# AccordionGroup

## Description

The AccordionGroup component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<AccordionGroup></AccordionGroup>
```

## Example 

<Canvas of={AccordionGroupStories.Default} />
<Controls of={AccordionGroupStories.Default} />
