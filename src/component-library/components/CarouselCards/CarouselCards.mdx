import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as CarouselCardsStories from './CarouselCards.stories'

<Meta of={CarouselCardsStories} />

# CarouselCards

## Description

The CarouselCards component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<CarouselCards></CarouselCards>
```

## Example 

<Canvas of={CarouselCardsStories.Default} />
<Controls of={CarouselCardsStories.Default} />
