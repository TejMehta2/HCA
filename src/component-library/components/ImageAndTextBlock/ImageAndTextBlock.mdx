import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as ImageAndTextBlockStories from './ImageAndTextBlock.stories'

<Meta of={ImageAndTextBlockStories} />

# ImageAndTextBlock

## Description

The ImageAndTextBlock component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<ImageAndTextBlock></ImageAndTextBlock>
```

## Example 

<Canvas of={ImageAndTextBlockStories.Default} />
<Controls of={ImageAndTextBlockStories.Default} />
