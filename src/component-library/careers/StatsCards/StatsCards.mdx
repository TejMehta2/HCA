import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as StatsCardsStories from './StatsCards.stories'

<Meta of={StatsCardsStories} />

# StatsCards

## Description

The StatsCards component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<StatsCards></StatsCards>
```

## Example 

<Canvas of={StatsCardsStories.Default} />
<Controls of={StatsCardsStories.Default} />
