import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as PhoneFieldStories from './PhoneField.stories'

<Meta of={PhoneFieldStories} />

# PhoneField

## Description

The PhoneField component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<PhoneField></PhoneField>
```

## Example 

<Canvas of={PhoneFieldStories.Default} />
<Controls of={PhoneFieldStories.Default} />
