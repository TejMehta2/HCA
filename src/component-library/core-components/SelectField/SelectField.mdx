import { Canvas, Controls, Meta } from '@storybook/blocks'
import * as SelectFieldStories from './SelectField.stories'

<Meta of={SelectFieldStories} />

# SelectField

## Description

The SelectField component is...

It is intended to be used in...

It can be composed like this:

``` tsx
<SelectField></SelectField>
```

## Example 

<Canvas of={SelectFieldStories.Default} />
<Controls of={SelectFieldStories.Default} />
