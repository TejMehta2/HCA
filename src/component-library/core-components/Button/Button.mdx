import { Canvas, Controls, Meta } from '@storybook/blocks';
import * as ButtonStories from './Button.stories';

<Meta of={ButtonStories} />

# Button

The Button component is intended apply styles by wrapping a button, JSS Link or Link tag (in the `children` prop). The styles will then target the button or anchor tag that is rendered.

The theme is built up of the button color - dark or light and variation - outline or full. There are variations of dark and light for the social buttons which have their own theme. This is controlled by the `theme` prop.

The color theme (light/dark) of the button can be inherited from the cascade theme by setting a theme prop which does not include "dark" or "light", e.g. "full" or "outline".

The size prop controls the button padding to show the appropriate button size - small or large.

To use an icon include it before or after the text content in your button.

```tsx
const exampleClick = () => {
  alert('Button was clicked');
};

<Button theme="full-dark" size="large">
  <button onClick="exampleClick">
    <span>Button Text</span>
    <Icons iconName="iconPhone" />
  </button>
</Button>;
```

```tsx
import { Link as JssLink } from '@sitecore-jss/sitecore-jss-nextjs';
<Button theme="full-dark" size="large">
  <JssLink field={props.fields.PromoLink}>
    <Icons iconName="iconPhone" />
    <span>Button Text</span>
  </JssLink>
</Button>;
```

```tsx
import Link from 'next/link';
<Button theme="full-dark" size="large">
  <Link href="/test">
    <Icons iconName="iconPhone" />
    <span>Button Text</span>
  </Link>
</Button>;
```

For loading buttons set the loading prop on the `Button` component.

```tsx
<Button theme="full-dark" size="large" loading={loading}>
  <JssLink field={props.fields.Link}>
    <span>'test text'</span>
  </JssLink>
</Button>
```

<Canvas of={ButtonStories.LargeFullDark} />
<Controls of={ButtonStories.LargeFullDark} />
